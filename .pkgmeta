package-as: HandyNotes_Homestead

enable-nolib-creation: no

externals:
  Libs/LibStub: https://repos.wowace.com/wow/libstub/trunk
  Libs/CallbackHandler-1.0: https://repos.wowace.com/wow/callbackhandler/trunk/CallbackHandler-1.0
  Libs/AceDB-3.0: https://repos.wowace.com/wow/ace3/trunk/AceDB-3.0
  Libs/AceEvent-3.0: https://repos.wowace.com/wow/ace3/trunk/AceEvent-3.0

# CF-only upload metadata: sent as a CurseForge relation when a CF project id
# is active (same pattern as Homestead's .pkgmeta — a page listing, not a
# packaging instruction).
required-dependencies:
  - handynotes

# RELEASE_NOTES.html is generated at CI time (extracted from CHANGELOG.md and
# converted from markdown to HTML by the release workflow). CF's API claims to
# accept changelogType: markdown but renders it as plain text; HTML is the only
# format that renders correctly on both CurseForge and Wago.
manual-changelog:
  filename: RELEASE_NOTES.html
  markup-type: html

# Repo-meta files: keep out of the packaged addon. HNH_Dev is the nested
# private tracker repo and is untracked in git, so CI checkouts never contain
# it — listed anyway so a local packager run can't ship it either.
ignore:
  - .github
  - .gitignore
  - .luacheckrc
  - .luarc.json
  - .pkgmeta
  - CHANGELOG.md
  - RELEASE_NOTES.md
  - RELEASE_NOTES.html
  - README.md
  - HNH_Dev
